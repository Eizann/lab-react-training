import React from 'react';

const NumbersTable = ({ limit }) => {
  const draw = () => {
const i = 0;
  return (
    <div>
        {while ({i} < limit) {
            
        }}
    </div>
  );
}

export default NumbersTable;
